org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.caching=true
org.gradle.configuration-cache=false

# ── Mod identity ────────────────────────────────────────────────
# Rename these for your own mod. `mod_id` must be lowercase a-z/0-9/_ and
# must match the Java mixin package + resource folder (assets/<mod_id>).
mod_id=enoughfolders
mod_name=Enough Folders
mod_version=1.0.2
mod_group=com.vodmordia.enoughfolders
mod_author=vodmordia
mod_description=Enough Folders allows you to organize your JEI items into folders for better organization.
mod_license=All Rights Reserved
